#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 *
 * @a - an integar
 * @b - an integar
 *
 * Return: sum
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
