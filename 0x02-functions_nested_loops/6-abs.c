#include "main.h"

/**
 * _abs - Computes the absolute value
 *
 * @i: input number as an integer.
 *
 * Return: absolute value
 */
int _abs(int)
{
	if (i > -1)
	{
		return(i);
	}
	else
	{
		return (i * -1);
	}
}
